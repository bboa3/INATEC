# Migration `20201101233229-update-subject`

This migration has been generated by Arlindo jose at 11/2/2020, 1:32:30 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Subjects" DROP CONSTRAINT "Subjects_classId_fkey"

ALTER TABLE "public"."Subjects" DROP CONSTRAINT "Subjects_userId_fkey"

CREATE TABLE "public"."Subject" (
"id" text   NOT NULL ,
"titleType" text   NOT NULL ,
"title" text   NOT NULL ,
"module" text   NOT NULL ,
"pdf" text   ,
"description" text   NOT NULL ,
"comments" jsonb   ,
"userId" text   NOT NULL ,
"name" text   NOT NULL ,
"teacher" boolean   NOT NULL ,
"avatar" text   NOT NULL ,
"classId" text   NOT NULL ,
"created_at" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"updated_at" timestamp(3)   NOT NULL ,
PRIMARY KEY ("id")
)

DROP TABLE "public"."Subjects"

ALTER TABLE "public"."Subject" ADD FOREIGN KEY("userId")REFERENCES "public"."Users"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."Subject" ADD FOREIGN KEY("classId")REFERENCES "public"."Class"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201101192712-subjects..20201101233229-update-subject
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model Users {
   id          String   @id @default(uuid())
@@ -16,9 +16,9 @@
   gender      String 
   password    String
   teacher     Boolean
   avatar      String? 
-  subjets     Subjects[]
+  subjets     Subject[]
   class       Class? @relation(fields: [classId], references: [id])
   classId     String?
   created_at  DateTime @default(now()) 
   updated_at  DateTime @updatedAt
@@ -31,12 +31,12 @@
   time     String 
   year     String
   user     Users[]
   schedule Json
-  Subjects Subjects[]
+  Subjects Subject[]
 }
-model Subjects {
+model Subject {
   id          String   @id @default(uuid())
   titleType   String
   title       String
   module      String
@@ -46,9 +46,9 @@
   user        Users    @relation(fields: [userId], references: [id])
   userId      String
   name        String
   teacher     Boolean
-  avatar      String?
+  avatar      String
   class       Class    @relation(fields: [classId], references: [id])
   classId     String
   created_at  DateTime @default(now()) 
   updated_at  DateTime @updatedAt
```


